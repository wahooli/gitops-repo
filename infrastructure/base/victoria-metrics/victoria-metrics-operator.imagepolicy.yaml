apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: victoria-metrics-operator
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: victoria-metrics-operator
  policy:
    semver:
      range: x.x.x
