apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: victoria-logs-single
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: victoria-logs-single
  policy:
    semver:
      range: x.x.x
