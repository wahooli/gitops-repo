image:
  tag: "9.20"

hostname: bind9

args:
- -g
- '-4'
- -c
- /etc/bind/named.conf

resources:
  limits:
    memory: 512Mi
  requests:
    memory: 300Mi

bind9:
  namedConfGenerator:
    enabled: true
  zoneCopy:
    enabled: true
    overwrite: false
