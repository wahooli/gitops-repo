apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: vmalert-vmetrics
  namespace: alerting
  annotations:
    external-dns.alpha.kubernetes.io/target: gw.${domain_absolutist_it:=absolutist.it}
spec:
  parentRefs:
  - name: envoy-gw-private
    namespace: envoy-gateway-system
  hostnames:
  - "metrics-alerts.${domain_absolutist_it:=absolutist.it}"
  rules:
  - backendRefs:
    - name: vmalert-vmetrics-nas
      port: 8080
