apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: longhorn
  namespace: flux-system
spec:
  dependsOn:
    - name: traefik
    - name: cert-manager
  interval: 5m
  path: "./infrastructure/longhorn"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system