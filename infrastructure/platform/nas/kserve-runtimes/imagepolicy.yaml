apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: llama-cpp-cuda
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: llama-cpp-cuda
  filterTags:
    pattern: '^server-cuda-b(?P<build>\d+)$'
    extract: '$build'
  policy:
    numerical:
      order: asc
