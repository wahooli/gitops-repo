apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: tautulli
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: tautulli
  policy:
    semver:
      range: 2.x.x
