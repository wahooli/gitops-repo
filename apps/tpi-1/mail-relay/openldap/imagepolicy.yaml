apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: openldap
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: openldap
  policy:
    semver:
      range: x.x.x
