apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls-homeassistant-ingress
  namespace: default
spec:
  secretName: tls-homeassistant-ingress
  commonName: ha.absolutist.it
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  dnsNames:
  - ha.absolutist.it
  - ha.wahoo.li
