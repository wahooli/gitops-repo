apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: docker-mailserver
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: docker-mailserver
  policy:
    semver:
      range: x.x.x
