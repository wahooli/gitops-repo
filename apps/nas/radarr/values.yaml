image:
  tag: "5.8.3" # {"$imagepolicy": "flux-system:radarr:tag"}

podAnnotations:
  backup.velero.io/backup-volumes: config
global:
  labels:
    backupWeekly: "true"
    backupDaily: "true"
    app.kubernetes.io/part-of: radarr

# resources:
#   requests:
#     memory: "256Mi"
#     cpu: "400m"
#   limits:
#     memory: "3072Mi"
#     cpu: "1000m"
persistence:
  config:
    enabled: true
    mount:
    - path: /config
    spec:
      isPersistentVolumeClaim: true
      accessModes:
      - "ReadWriteOnce"
      resources:
        requests:
          storage: "2Gi"
  data:
    enabled: true
    mount:
    - path: /data/
    spec:
      hostPath:
        path: ${radarr_data_host_path}
        type: Directory
ingress:
  main:
    enabled: true
    className: istio
    labels:
      velero.io/exclude-from-backup: "true"
    annotations:
      cert-manager.io/cluster-issuer: letsencrypt-staging
    hosts:
    - host: radarr.wahoo.li
      paths:
      - path: "/"
        pathType: "Prefix"
    - host: radarr.test.absolutist.it
      paths:
      - path: "/"
        pathType: "Prefix"
    tls:
    - secretName: tls-radarr-ingress
      hosts:
      - radarr.wahoo.li
      - radarr.test.absolutist.it
