apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: deluge
  namespace: default
  annotations:
    external-dns.alpha.kubernetes.io/target: gw.${domain_absolutist_it:=absolutist.it}
spec:
  parentRefs:
  - name: envoy-gw-private
    namespace: envoy-gateway-system
  hostnames:
  - "deluge.${domain_absolutist_it:=absolutist.it}"
  - "deluge.${domain_wahoo_li:=wahoo.li}"
  rules:
  - backendRefs:
    - name: deluge
      port: 8112
