apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hermes-agent-data
  namespace: hermes-agent
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: topolvm-fast
  resources:
    requests:
      storage: 10Gi
