apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: whisper-asr-models
  namespace: default
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: topolvm-fast
  resources:
    requests:
      storage: 10Gi
