apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: wireguard
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: wireguard
  policy:
    semver:
      range: x.x.x
