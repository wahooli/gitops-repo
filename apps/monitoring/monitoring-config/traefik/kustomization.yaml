apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: flux-system
resources:
  - servicemonitor.yaml
configMapGenerator:
  - name: traefik-grafana-dashboard
    files:
      - grafana-dashboards/traefik.json
    options:
      labels:
        grafana_dashboard: traefik