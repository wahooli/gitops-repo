apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls-nextcloud-ingress
  namespace: ext
spec:
  secretName: tls-nextcloud-ingress
  commonName: cloud.absolutist.it
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
  - cloud.absolutist.it
  - cloud.wahoo.li
