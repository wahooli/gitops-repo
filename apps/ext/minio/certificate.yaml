apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls-minio-ingress
  namespace: ext
spec:
  secretName: tls-minio-ingress
  commonName: minio.absolutist.it
  issuerRef:
    name: letsencrypt-staging
    kind: ClusterIssuer
  dnsNames:
  - minio.absolutist.it
  - minio.wahoo.li
