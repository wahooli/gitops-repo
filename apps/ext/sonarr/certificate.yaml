apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls-sonarr-ingress
  namespace: ext
spec:
  secretName: tls-sonarr-ingress
  commonName: sonarr.absolutist.it
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
  - sonarr.absolutist.it
  - sonarr.wahoo.li
