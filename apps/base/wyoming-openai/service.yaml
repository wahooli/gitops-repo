apiVersion: v1
kind: Service
metadata:
  name: wyoming-openai
  namespace: homeassistant-assist
  labels:
    app.kubernetes.io/name: wyoming-openai
spec:
  type: ClusterIP
  ports:
  - name: wyoming
    port: 10200
    targetPort: wyoming
    protocol: TCP
  selector:
    app.kubernetes.io/name: wyoming-openai
