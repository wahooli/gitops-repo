apiVersion: v1
kind: Service
metadata:
  name: hermes-agent
  namespace: hermes-agent
  labels:
    app.kubernetes.io/name: hermes-agent
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 80
    targetPort: api
    protocol: TCP
  selector:
    app.kubernetes.io/name: hermes-agent
