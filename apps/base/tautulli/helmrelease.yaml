apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: base-tautulli
  namespace: flux-system
spec:
  releaseName: base-tautulli
  chart:
    spec:
      reconcileStrategy: Revision
      chart: ./charts/tautulli
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  install:
    remediation:
      retries: -1
