apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: whisper-asr-gpu
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: whisper-asr
  filterTags:
    pattern: '^v(?P<major>\d+)\.(?P<minor>\d+)\.(?P<patch>\d+)-gpu$'
    extract: '$major.$minor.$patch'
  policy:
    semver:
      range: '>=1.0.0'
