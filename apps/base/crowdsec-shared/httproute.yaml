apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: crowdsec-api
  namespace: crowdsec
spec:
  parentRefs:
  - name: envoy-gw-private
    namespace: envoy-gateway-system
  hostnames:
  - "crowdsec-api.${domain_absolutist_it:=absolutist.it}"
  rules:
  - backendRefs:
    - name: crowdsec-service
      port: 8080
